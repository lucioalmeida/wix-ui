import { TooltipTheme } from './theme';
export declare const styles: (theme: TooltipTheme) => {
    tooltip: {
        backgroundColor: string;
        borderWidth: string;
        borderStyle: string;
        borderColor: string;
        borderRadius: string;
        padding: string;
    };
};
